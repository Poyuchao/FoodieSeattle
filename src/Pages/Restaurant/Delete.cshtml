@page "{id}"
@using FoodieSeattle.WebSite.Services
@inject RestaurantService _RestaurantService
@model FoodieSeattle.WebSite.Pages.Restaurant.DeleteModel


@{
    ViewData["Title"] = "Delete page";
}
<p style="line-height:5.5em;"> </p>
<!--CRUDi: Delete page to show: contains some data of the selected Restaurant and a button to confirm Delete-->
<h1>Delete</h1>

<div class="container">
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @Html.HiddenFor(x => x.Restaurant.Id)

        <div class="form-group">
            <label asp-for="Restaurant.Title" class="control-label">Restaurant Name</label>
            <input asp-for="Restaurant.Title" class="form-control" readonly />
            <span asp-validation-for="Restaurant.Title" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.Type" class="control-label">Cuisine Type</label>
            <input asp-for="Restaurant.Type" class="form-control" readonly />
            <span asp-validation-for="Restaurant.Type" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.Neighborhood" class="control-label">Restaurant Neighborhood</label>
            <input asp-for="Restaurant.Neighborhood" class="form-control" readonly />
            <span asp-validation-for="Restaurant.Neighborhood" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.City" class="control-label">City</label>
            <input asp-for="Restaurant.City" class="form-control" readonly />
            <span asp-validation-for="Restaurant.City" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.State" class="control-label">State</label>
            <input asp-for="Restaurant.State" class="form-control" readonly />
            <span asp-validation-for="Restaurant.State" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.Address" class="control-label">Address</label>
            <input asp-for="Restaurant.Address" class="form-control" readonly />
            <span asp-validation-for="Restaurant.Address" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.Description" class="control-label">Description</label>
            <input asp-for="Restaurant.Description" class="form-control" readonly />
            <span asp-validation-for="Restaurant.Description" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.Url" class="control-label">Link to Restaurant Homepage</label>
            <input asp-for="Restaurant.Url" class="form-control" readonly />
            <span asp-validation-for="Restaurant.Url" class="text-danger"></span>
        </div>

        <br />

        <div class="form-group">
            <label asp-for="Restaurant.Image" class="control-label">Link to Restaurant Image</label>
            <input asp-for="Restaurant.Image" class="form-control" readonly />
            <span asp-validation-for="Restaurant.Image" class="text-danger"></span>
        </div>

        <br />

        <h1>Confirm Delete of this item</h1>
        <div class="form-group">
            <a type="button" value="Cancel" class="= btn btn-primary" asp-page="/Restaurant/Index">Cancel</a>
            <input type="submit" value="Delete" class="btn btn-primary" />
        </div>
    </form>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}